# Copy to .env and adjust as needed
OPENSEARCH_HOST=localhost
OPENSEARCH_PORT=9200
LOG_INDEX=logs
RETENTION_DAYS=7
SYSLOG_HOST=0.0.0.0
SYSLOG_PORT=5514
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost,https://localhost
ALERT_WEBHOOK_URL=

# Frontend (dev). Leave empty in Docker so UI calls /api on same origin.
VITE_API_URL=http://127.0.0.1:8000
